{
  "_comment": "Merge THIS into ~/.workbuddy/settings.json under hooks.PreCompact. WorkBuddy follows the Claude Code hook spec. Do not overwrite — append.",
  "hooks": {
    "PreCompact": [
      {
        "matcher": "auto",
        "hooks": [
          {
            "type": "command",
            "command": "python \"D:/.agents/plugins/session-summarizer/scripts/precompact_hook.py\"",
            "timeout": 60000
          }
        ]
      }
    ]
  }
}
